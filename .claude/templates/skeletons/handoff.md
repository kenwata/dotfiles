# HANDOFF(最終更新: {{date}})

<!--
セッション終了時に、最後に完了したタスク、仕掛かり中、次の一手、要確認のいずれかが変わった場合、または TODO.md §0 のコールドスタート確認で不足が見つかった場合は、この構造で全体上書きしてコミットする(追記しない)。どちらでもなければ書き換えない。常に1画面＝40行以内に保つ(この規約コメントを除いた本文の行数。コメントを含めると生成直後から予算を使い切るため)。
コード・長文は貼らない。`path/to/file:行番号` で参照する。
個人的な学び・環境固有の情報はここに書かない(auto memory の領分)。
会話にしか無い事実を書くのは、次セッションの最初の一手または同じ T の再開に必要(否定済みの経路を含む)で、既存の永続状態から復元できない場合だけ。重要そう・再調査に手間がかかる、だけでは書かない。
全体上書きで仕掛かり中から消える行のうち、その T の完了後も有効な事実は上書き前に担当文書へ移す(設計上の制約→設計書、構造上の制約→docs/architecture.md、判断→docs/decisions.md、完了した T の経緯→コミット本文「対応:」)。要確認はユーザー判断待ち専用であり、移し先にしない。
要確認の規約(回収されずに溜まり続けることを防ぐ。回収の手順の正は ~/.claude/commands/follow-up.md と ~/.claude/commands/execute-task.md):
  - 入れてよいのは、利用者にしか決められない問いだけ。問いの形(何を決めてほしいか・選択肢・推奨)で書く。1項目1行、束ねない。
  - 行の先頭に回収点を固定書式で書く。`[回収: T<n> 着手前]`(/execute-task T<n> が着手前に必ず問う)か `[回収: 次の /follow-up]`(次の総点検が必ず問う)の2値だけ。回収点を書けない項目は要確認ではない。「急がない」等の優先度ラベルは使わない。
  - 決定済みの制約・承認・利用者決定は問いではないので、docs/decisions.md に1行書く。将来のタスクを縛るなら、タスクID列にその T<n> を書く(/execute-task T<n> が着手前にその列を読む)。
  - どの時点の判断にも効かない気づきは、コミット本文「対応:」に書き、ここには残さない。
  - 唯一の例外: /breakdown が見つけた設計書の事実誤りの是正依頼は `[回収: 次の /follow-up]` を付けてここに書く(/follow-up の是正段が利用者に問わずに直す)。
  - 全体上書きの時、要確認の行は逐語で持ち越す(言い換えない・黙って落とさない)。消してよいのは、決着を docs/decisions.md に書いた項目と、既に決着済みと実測できた項目だけ。
テンプレート(この構造そのものが雛形。値を差し替えて上書きする):
  # HANDOFF(最終更新: YYYY-MM-DD)
  目標: (このタスク群が達成すべきアウトカムを1行で)
  ## 最後に完了したタスク
  - （1〜数行で要約。詳細はコミットSHA(例: `abc1234`)で参照する — 作業ログの実体は git log が正）
  ## 仕掛かり中
  - タスクID / 中断点 / 途中成果物のパス / 棄却済みの経路と理由(あれば)（なければ「なし」）
  - 設計の穴で止まった時は穴の記録を各1行で足す(/amend の一次情報。推測でなく観測で書く): 観測した事実と証拠(path:行 かコマンドと結果) / 矛盾または欠落している設計書の節 / 満たせない完了条件の項目。この時の次の一手は `/amend T<n>`
  ## 次セッションの最初の一手
  - （具体的に1つ）
  ## 要確認(ユーザー判断待ち)
  - [回収: T<n> 着手前 | 次の /follow-up] （何を決めてほしいか・選択肢・推奨。なければ「なし」）
-->

目標: (このタスク群が達成すべきアウトカムを1行で)

## 最後に完了したタスク

- なし(初期化直後)

## 仕掛かり中

- なし

## 次セッションの最初の一手

- (最初のタスクをここに書く)

## 要確認(ユーザー判断待ち)

- なし
