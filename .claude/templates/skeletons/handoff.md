# Handoff

<!--
規約(このコメントは消さない):
- 全体で 60 行以内を維持する。超えたら古いエントリを .claude/archive/handoff.md へ
  一字一句そのまま移動する(要約・言い換え禁止。詳細は rules/growing-docs.md)
- コードや長文は貼らない。`path/to/file:行番号` で参照する
- 「完了」欄は次回の更新時に削除する(履歴は git log に残る)
- 個人的な学び・環境固有の情報はここに書かない(auto memory の領分)
- セッション終了時に更新し、コミットに含める
-->

更新: {{date}}

## 目標

(このタスク群が達成すべきアウトカムを 1-2 行で)

## 次の一手

- (次のセッションが最初に着手すること 1 件。対象パス付きで具体的に)

## TODO

- [ ] (項目 — 関連: `path/to/file:行`)

## 進行中の決定・注意

- (次セッションに必要な最小限のみ。決定済み事項と、踏むと危ない地雷)

## 完了(直近サイクルのみ・次回更新で削除)

- [x] (項目 (YYYY-MM-DD))
