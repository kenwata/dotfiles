<!--
規約(このコメントは消さない):
- 本表は **派生ビュー** である。正は各設計書の実体(冒頭の `# 設計: <表題>` 見出しと
  「全体構想」行)と git の追加履歴。食い違ったら設計書側を正として本表を直す。
  正でないため、本表の記述を根拠に設計判断をしない(必ず設計書本体を開く)。
- 目的は 2 つ。①設計書がどの時系列で作られたかをファイル名を開かずに追えること
  ②ファイル名が似ていても表題で内容を判別できること。この 2 つを満たさない省略はしない。
- 行は **追記のみ・時系列順(古い順)**。既存行の並べ替え・番号の振り直し・削除は禁止。
  設計を廃止した場合も行は残し、表題の末尾に `(廃止)` と書く(通史が消えると本表の意味が失われる)。
- **ローテーション対象外**。`.claude/rules/growing-docs.md` の完了分退避を適用しない
  (完了した設計書ほど消えていくと通史が作れない。TODO.md との違いはここ)。
- 状態(未着手 / 完了)は書かない。状態は `TODO.md` と `HANDOFF.md` の領分
  (`~/.claude/templates/BLUEPRINT.md` §6「五文書の境界」)。本表に状態列を足さない。
- 列の意味:
  - 作成日: 設計書が最初にコミットされた日(`YYYY-MM-DD`)。`/elaborate` は索引と設計書を
    同一コミットで着地させるため、生成時の日付(`date +%Y-%m-%d`)を書けば一致する。
    日付をまたいでコミットした等でずれた場合は git 側を正として直す
    (`git log --reverse --diff-filter=A --date=short --format='%ad' -- docs/design/<slug>.md`)
  - フェーズ: `plan.md` のフェーズ見出しを逐語で写す(例 `フェーズ 1: 構成分割と書き方の規約適合`)。
    `plan.md` を持たないプロジェクト、または対応フェーズが無い設計書は `—`。重複してよい
    (1 フェーズが 2 つの設計書に割れる場合がある)
  - 設計書: `docs/design/` からの相対リンク
  - 表題: 設計書の `# 設計: <表題>` をそのまま写す。slug の言い換えで済ませない
    (ファイル名で判別できないことを補うのが本列の役目)
  - T: `/breakdown` が採番したタスクID範囲(例 `T1〜T6`)。採番前は `—`
- 更新の配線: 新規行の追記は `/elaborate`、`T` 列の記入は `/breakdown`、
  漏れ・食い違いの検出は `/follow-up` の機械チェック⑩。手で書き足してもよいが、
  書式は上記に従う。
-->

# 設計書索引

| 作成日 | フェーズ | 設計書 | 表題 | T |
| ------ | -------- | ------ | ---- | - |
| {{date}} | {{phase}} | [{{slug}}]({{slug}}.md) | {{title}} | — |
<!-- 追記位置: 新規行はこの直前 -->
