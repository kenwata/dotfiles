<!--
規約(このコメントは消さない):
- 行数予算: 250 行(安全上限)。超過しそうな時は .claude/rules/growing-docs.md の手順で、
  完了タスクを古い順に .claude/archive/TODO.md へ一字一句そのまま移動する(要約禁止)。
  移動は「タスク表の行」と「対応する完了条件ブロック」をセットで行い、archive 側にも
  同じ表ヘッダを置く。計画の全タスクが移動し終えたら、計画テーブルの該当行も一緒に移す。

- ローテーションは予算超過時だけでなく **セッション終了時にも走らせる**(growing-docs.md の
  既定契機に対する、このファイル固有の上書き)。対象は「前セッション以前に完了したタスク」:
  /follow-up 手順2 で確定したセッション開始時の HEAD を使い、
  `git show <開始HEAD>:TODO.md` の時点で既に [x] だったタスクを移動する。
  当該セッションで [x] にしたタスクは残す(/follow-up 手順3④ の HANDOFF.md との突合に使うため)。
  理由: 完了タスクが予算いっぱいまで居座ることが肥大化の実体であり、毎セッション読む
  working file には未完了タスクと直近の完了分だけあればよい。

- ID は 2 系統。役割が違うので混ぜない。
  - T<n>: タスク管理・外部参照の接続キー。プロジェクト内の通し番号。再利用・振り直し禁止。
    archive へ移動しても欠番のまま維持する(git log --grep='T<n>' との対応を壊さないため)。
    新規採番は本ファイルと .claude/archive/TODO.md の最大番号 +1 から。
    コミット要約・docs/decisions.md・HANDOFF.md が参照するのは常にこちら。
  - #<n> / #<n>-<m>: グルーピングとソートのための表示専用インデックス(章番号・節番号に相当)。
    #<n> は計画の通し番号、#<n>-<m> は計画 #<n> 内の通し番号。同じく再利用・振り直し禁止。
    外部文書からこの番号を参照しない(T<n> が正)。

- 追記位置: 新規行・新規ブロックは必ず対応する追記位置マーカーの直前に入れる。既存行の
  並べ替え・番号の振り直し・表の途中への挿入は禁止。T<n> は飛んでよい(#<n>-<m> が密になる)。

- 難易度: 低 / 中 / 高 の 3 値。規模ではなく exec フェーズの実行モデルを選ぶ判断材料。
  下記のモデル対応は **目安であり拘束しない**(難易度を入力に実行時に判断する。表の
  引き当てではない)。advisor だけは難易度によらず fable 固定。
  - 低 — exec は sonnet 目安
         単一ファイル / 既存パターンの踏襲 / 完了条件がコマンド 1 本で yes-no 判定できる
  - 中 — exec は opus 目安
         複数ファイル横断 / 設計判断を 1 つ以上含む / 既存パターンの応用が必要
  - 高 — exec は fable 目安
         仕様が未確定で探索を伴う / 非自明なデバッグ / 後戻りコストの高い不可逆変更 /
         完了条件に [subjective] を含む
  計画テーブルの難易度は配下タスクの最大値。plan フェーズは難易度によらず opus 目安。

- 完了条件: タスク表のセルには書かず、表の下の完了条件ブロックに書く(列崩れを防ぐため)。
  書式: **#<n>-<m> / T<n>** — 完了条件: <検証可能な条件>
  機械的に yes/no を出せない定性条件は [subjective] と明記する。
  完了条件の正はこのファイル。docs/design/<slug>.md へ転記しない。

- 状態は [ ] 未着手 / [x] 完了 の 2 値のみ。仕掛かり中の中断点はここに書かない
  (HANDOFF.md の領分)。
-->

# TODO

## §0 セッションプロトコル

- 開始時に読む: このファイルと `HANDOFF.md` の 2 つ
- タスク完了ごと: ①該当タスクの状態を `[x]` に更新(配下の全タスクが `[x]` になったら計画テーブルの該当行も `[x]` に更新) ②git commit(要約に `T<n>` を含める。変更を生まないタスクは `git commit --allow-empty` で記録を残す)
- 終了時: `/follow-up` を実行する — 計画と成果の差分を検査・是正した上で、終了手順(状態更新・`HANDOFF.md` の全体上書き・`docs/decisions.md`・コミット)まで行う。手順の実体は `~/.claude/templates/BLUEPRINT.md` §6 が正

## 計画

| #  | 計画 | 設計 | 難 | 状態 |
| -- | ---- | ---- | -- | ---- |
| #1 | {{slug}} | docs/design/{{slug}}.md | {{難易度}} | [ ] |
<!-- 追記位置(計画): 新規行はこの直前 -->

## #1 {{slug}}

| #    | T  | タスク | 難 | 状態 |
| ---- | -- | ------ | -- | ---- |
| #1-1 | T1 | {{task_name}} | {{難易度}} | [ ] |
<!-- 追記位置(#1 タスク): 新規行はこの直前 -->

**#1-1 / T1** — 完了条件: {{verifiable_condition}}
<!-- 追記位置(#1 完了条件): 新規ブロックはこの直前 -->
