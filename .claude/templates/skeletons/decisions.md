<!--
規約(このコメントは消さない):
- 用途: 仕様解釈・設計からの逸脱・ユーザー決定を1行で記録する判断ログ。
  「今の状態」を書く HANDOFF.md とは別物 — こちらは「過去になぜそう決めたか」の記録。
- 書きトリガー: タスク実行または軽量なセッション終了処理(CLAUDE.md の「セッション運用」節、
  または TODO.md を持つプロジェクトでは TODO.md §0)に配線する。該当する判断がない回は追記しない。
- 読みトリガー: セッション開始時に読む必要はない。過去の経緯・判断理由を調査する時に検索して
  参照する。唯一の定常の読みトリガーは /execute-task T<n> の着手前の関門で、タスクID列が
  その T<n> の行を抜き出し、対象タスクを縛る決定済みの制約として読む。したがって将来の
  タスクを縛る決定(承認の範囲、着手の前提条件など)は、タスクID列にそのタスクを書く
  (機械的に抜き出せるよう、`T36〜T42` のような範囲ではなく `T36, T38` のように個々の ID で書く)。
- 追記専用(append-only)。既存行の編集・削除・要約はしない。
-->

| 日付 | タスクID | 判断内容 | 理由 |
| ---- | -------- | -------- | ---- |
