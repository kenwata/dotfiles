<!--
規約(このコメントは消さない):
- 安定文書: why / what のみを書く。状態・進捗を書かない(状態は HANDOFF.md、実行は TODO.md)。
- 判断を変更する時は docs/decisions.md に 1 行追記してから本文を更新する(経緯は decisions が正)。
- 作成時の義務: プランファイルの転記・要約ではなく、会話コンテキストを一次情報として
  プランより詳細に書く。プランで省略された前提・境界条件・検討済み代替案を復元して残す。
- 命名: 未定義の省略ID(M0, P1 等)を使わない。説明的な名前で書く(例: 「フェーズ1: スキーマ移行」)。
  例外は TODO.md 規約で定義済みの ID(タスクID T<n>、グルーピング用インデックス #<n>/#<n>-<m>)のみ。
- 全体構想との境界: プロジェクトが plan.md(ルート直下の全体構想。BLUEPRINT §6 の五層目)を持つ場合、
  本書はその 1 フェーズの詳細であり、冒頭の「全体構想」行で該当フェーズを指す。フェーズ構成・スコープ・
  方針など全体に関わる内容は本書に書かず plan.md 側を更新する(転記しない)。plan.md が無い
  プロジェクト(plan mode 起点)では「全体構想: なし」と書く。
- 「全体構想」行の書式は固定: `全体構想: plan.md §<節番号> / <フェーズ見出しの逐語>`
  (例 `全体構想: plan.md §9 / フェーズ 1: 構成分割と書き方の規約適合`)。区切りは半角スペース+
  スラッシュ+半角スペース。節番号だけでは足りない — plan.md はフェーズを 1 節 1 フェーズで
  書くとは限らず(`§9 フェーズ構成` の下に `### フェーズ 0` 〜 `### フェーズ 4` を並べる形が実在する)、
  節番号のみでは指しているフェーズが特定できない。この書式に揃える理由は、/follow-up の機械チェック⑨が
  節とフェーズ見出しの実在を検査し、docs/design/index.md のフェーズ列がこの行から起こされること。
- 作成は /elaborate が行う。更新は変更の大きさで分かれる: 「目的」「スコープ / 非スコープ」が変わるなら
  /elaborate、「方針・構成」節の改訂で足りるなら /amend(正は ~/.claude/templates/BLUEPRINT.md §6
  「変更の三段分類」)。「タスク分解」節は /breakdown が埋め、/amend がタスクを足した時は /amend が
  直す(番号が連続しなければ `T42〜T71, T120` のように列挙で書く)。「方針・構成」の「実行者の裁量と停止条件」「段階と分解の指針」「落とし穴」は、
  /breakdown と /execute-task の読み手(通常実装の役割のモデル)が上位モデルへ戻らずに走り切るための節で、
  /breakdown は TODO.md の「共通の前提」からこれらの項を名指しする。段階を並べた計画では
  /breakdown が 1 回に 1 段階だけを分解し、この節に `段階 <n>: 未分解` を残す。本書を新規に作ったら
  docs/design/index.md(設計書索引。雛形は ~/.claude/templates/skeletons/design-index.md)へ 1 行足す
  — 索引は追記が配線されていないと即座に嘘になる。
- 本流と支線の区別は本書に書かない(全体構想の事実なので plan.md の `本流: §<節番号>` が正。
  本書の「全体構想」行がその節を指せば本流、それ以外を指せば支線)。
-->

# 設計: {{title}}

全体構想: {{plan.md §<節番号> / <フェーズ見出し> | なし}}

## 目的(アウトカム)

({{達成すべき状態を書く。ユーザー発言の生の意図は逐語で残す — 要約すると定性的な意図が落ちる}})

## スコープ / 非スコープ

- In: {{...}}
- Out(非目標。ゴールドプレーティングと暗黙の省略の両方を防ぐため明示する): {{...}}

## 方針・構成

({{方針と主要な構成。プランで省略された前提・境界条件・落とし穴も会話から復元して書く。**番号付きの項(`### 1. <見出し>`、`### 2. …`)で書く** — TODO.md の「共通の前提」と各タスクは、読む範囲を「第 n 項」で名指しする。変えてはならない契約(入出力の形、正とする文書の順序、異常な入力の扱いなど)は、名指しできるよう独立した項として立てる。下の 4 つの小見出しも、この通し番号の続きとして番号を振る}})

### 前提と確かめ方

({{この設計が成り立つために真でなければならない事実と、それを何で確かめるか。状態(確かめたかどうか)は書かない — 読むだけ(Read・Glob・Grep)で確かめられる前提は /elaborate がコミット前に確かめ、崩れていたら本書を直してから着地させる。実装しないと確かめられない前提は、その旨を書く(/breakdown が最初のタスクの完了条件に含める)}})

### 実行者の裁量と停止条件

({{実行者 = ~/.claude/templates/model-routing.md の通常実装の役割のモデル。上位モデルの判断なしに走り切れるよう、固定と裁量の境界をここで引く。判定を要する規則(段階の境界、採否、閾値)は本書で確定し、タスクには規則に当てはめる作業だけを残す。次の 5 点を箇条で書く:
- 本書が固定するもの: 変えてはならない契約(パス・引数・出力の形・閾値・節と順序・件数など)を、本書の項を指して列挙する
- 裁量に任せるもの: 実装の書き方、名前と分け方、文言、テストデータなど。実行者は、裁量で決めた点のうち後続のタスクや他の成果物が依存するもの(終了コード、ファイルの置き場、公開される名前など)を docs/decisions.md に 1 行残す(裁量に入れてよいのは、決めた結果がタスクの対象パスの中で閉じるものだけ。対象外へ波及しうる判断は本書で固定する)
- 本書が「現状」として書いた事実が現物と食い違った時の扱い
- 固定した契約を満たせない、または変更が要ると判断した時は、変更せずにそのタスクを止める(/execute-task が穴の記録を残し /amend へ渡す)。止めずに切り替えてよい例外があれば、条件ごと書く
- 迷った時にどちらへ倒すか}})

### 段階と分解の指針

({{/breakdown への指針。次を書く:
- 段階: 実装の順序のまとまり。1 段階 = 完了すると検証可能な中間状態に達する単位。/breakdown は 1 回に最初の未分解の 1 段階だけを TODO へ分解する(後半のタスクを、前半の実装で得た事実の前に確定させないため)。1 段階で足りる計画では段階を並べない
- 実行順序と依存
- 1 タスクの大きさの目安: この計画の単位で書く(例「レコード 5〜10 件の作成」「検証スクリプト 1 本とそのテスト」)。1 タスク = 1 セッションで完了する大きさ
- 分けるべき境界: 外部状態に触れるタスク、検証者や実行者が別のタスク(人が行うレビューなど)}})

### 落とし穴

({{実行者が踏みやすい誤りと、その避け方。会話や調査で分かったが、コードや文書を読むだけでは気付けないものを優先する}})

## 検討した代替案と不採用理由

| 代替案 | 不採用理由 |
| ------ | ---------- |
| {{...}} | {{...}} |

## タスク分解

- `TODO.md` の T{{n}}〜T{{m}}(完了条件はそちらが正。ここに転記しない)
  ({{段階を並べた計画では、この箇条を段階ごとの行に置き換える(この括弧書きは消す)。分解済みの段階は「段階 1: T{{n}}〜T{{m}}」、まだ分解していない段階は「段階 2: 未分解」と、行頭から固定書式で書く。行頭の「段階 <n>: 未分解」が残る間は TODO.md の計画行を完了にしない}})
